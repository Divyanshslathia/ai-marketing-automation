class BriefAgent:
    """
    Converts raw user inputs into a structured campaign brief.
    Output must be machine-readable (JSON-compatible dict).
    """

    def run(self, inputs: dict) -> dict:
        raise NotImplementedError("BriefAgent not implemented yet")
